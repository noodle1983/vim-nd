#!/bin/bash

SELF_PATH=$0
if [ -L $SELF_PATH ]; then
    SELF_PATH=`readlink $SELF_PATH`
fi

SELF_DIR=`dirname $SELF_PATH`
if [ ${SELF_DIR:0:1} != "/" ]; then
    SELF_DIR="$PWD/$SELF_DIR"
fi

$SELF_DIR/run_until_success git pull
